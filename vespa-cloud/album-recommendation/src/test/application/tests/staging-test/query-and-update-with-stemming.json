{
    "name": "Query after application upgrade",
    "comment": "The test steps here are run after the test deployment is upgraded to the next platform and/or application package.",
    "defaults": {
        "cluster": "default"
    },
    "steps": [
        {
            "name": "query for 'fighter'",
            "comment": "Consider removing stemming from the 'artist' field, and updating the expected ../hit.json to assert no hits;",
            "more-comment": "since the old (current) config is _with_ stemming, the doc will be fed _with_ stemming prior to upgrade,",
            "even-more-comment": "and it will match ('fighters' was stemmed to 'fighter'). This is different from the system-test.",
            "request": {
                "parameters": "../query.json"
            },
            "response": {
                "body": "../hit.json"
            }
        }
    ]
}
