<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright Vespa.ai. Licensed under the terms of the Apache 2.0 license. See LICENSE in the project root. -->
<services version="1.0" xmlns:deploy="vespa" xmlns:preprocess="properties" >

  <container id="default" version="1.0">

    <!-- Mock secrets for local testing -->
    <component id="secrets" class="ai.vespa.test.LocalSecrets" bundle="rag" />

    <!-- Setup OpenAI client - also works directly as a generator -->
    <component id="openai" class="ai.vespa.llm.clients.OpenAI">
      <config name="ai.vespa.llm.clients.llm-client">
        <apiKeySecretName>openAiKey</apiKeySecretName>
      </config>
    </component>

    <!-- Setup a custom generator -->
    <component id="prompt" class="ai.vespa.test.PromptGenerator" bundle="rag">
      <config name="ai.vespa.test.prompt-generator">
<!--        <prompt>Prepend the following paragraph with "foo": {context}</prompt>-->
        <promptTemplate>files/prompt.txt</promptTemplate>
      </config>
    </component>


    <document-api/>
    <search />
    <nodes>
      <jvm options="-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5005" />
      <node hostalias="node1" />
    </nodes>
  </container>

  <content id="msmarco" version="1.0">
    <redundancy>1</redundancy>
    <documents>
      <document mode="index" type="passage"/>
    </documents>
    <nodes>
      <node hostalias="node1" distribution-key="0" />
    </nodes>
  </content>

</services>
